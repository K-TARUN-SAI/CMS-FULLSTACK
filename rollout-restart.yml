- name: Restart CMS deployments to pull latest images
  hosts: local
  connection: local

  tasks:
    - name: Restart backend deployment
      command: kubectl rollout restart deployment/backend -n cms-app

    - name: Restart frontend deployment
      command: kubectl rollout restart deployment/frontend -n cms-app

    - name: Check pods
      command: kubectl get pods -n cms-app
